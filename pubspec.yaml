name: motion_exporter
description: "Record animated Flutter widgets and export transparent motion formats."
version: 0.0.1
homepage: https://github.com/KickNext/motion_exporter
repository: https://github.com/KickNext/motion_exporter
issue_tracker: https://github.com/KickNext/motion_exporter/issues
topics:
  - flutter
  - animation
  - apng
  - webp
  - export
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
screenshots:
  - description: Transparent Flutter motion export preview.
    path: screenshots/transparent_orbit.png

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  image: ^4.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
